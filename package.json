{
    "name": "ws-server",
    "version": "0.1.0",
    "description": "A WebSocket server in typescript.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "tsc",
        "watch": "tsc -w",
        "lint": "tslint -c tslint.json -p tsconfig.json",
        "start": "tsc -v && yarn build && node ./dist/index.js",
        "restart": "node ./dist/index.js"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/wwlib/ws-server"
    },
    "author": "Andrew Rapo",
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^10.5.3"
    },
    "dependencies": {
        "ws": "^6.0.0"
    }
}
